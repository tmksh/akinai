-- ============================================================
-- Migration 035: product_images にサムネイルURLを追加
-- ------------------------------------------------------------
-- アップロード/取り込み時にサーバー側で生成する一覧用サムネイル
-- （例: 長辺400px WebP）のURLを保持する。
-- 既存行は NULL のまま（表示側は thumbnail_url ?? url でフォールバック）。
-- ============================================================

ALTER TABLE product_images
  ADD COLUMN IF NOT EXISTS thumbnail_url TEXT;
